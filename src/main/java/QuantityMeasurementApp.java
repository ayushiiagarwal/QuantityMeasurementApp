public class QuantityMeasurementApp {
    public static void main(String[] args) {
        // Quantity<LengthUnit> length1 = new Quantity<>(1, LengthUnit.FEET);
        // Quantity<LengthUnit> length2 = new Quantity<>(12, LengthUnit.INCH);

        // System.out.println("is length equal? " + length1.equals(length2));

        // Quantity<WeightUnit> weight1 = new Quantity<>(1, WeightUnit.KILOGRAM);
        // Quantity<WeightUnit> weight2 = new Quantity<>(1000, WeightUnit.GRAM);

        // System.out.println("Is weight equal? " + weight1.equals(weight2));
        // System.out.println("Convert 1 foot to inches: " + length1.convertTo(LengthUnit.INCH));
        // System.out.println("Add lengths: " + length1.add(length2));
        // System.out.println("Add weights in grams: " + weight1.add(weight2, WeightUnit.GRAM));

        // Quantity<VolumeUnit> volume1 = new Quantity<>(1, VolumeUnit.LITRE);
        // Quantity<VolumeUnit> volume2 = new Quantity<>(2, VolumeUnit.MILLILITRE);
        // System.out.println(volume1.equals(volume2));

        // Quantity<VolumeUnit> volume3 = new Quantity<>(1, VolumeUnit.LITRE);
        // Quantity<VolumeUnit> volume4 = new Quantity<>(1000, VolumeUnit.MILLILITRE);
        // System.out.println(volume3.equals(volume4));

        // System.out.println(volume1.convertTo(VolumeUnit.GALLON));
        // System.out.println(volume2.add(volume3));
        // System.out.println(volume3.add(volume4, VolumeUnit.GALLON));

        // System.out.println("Subtract: " + length1.subtract(length2));
        // System.out.println("Subtract in inches: " + length1.subtract(length2, LengthUnit.INCH));

        // System.out.println("Division ratio: " + weight1.divide(weight2));

        
    }
}